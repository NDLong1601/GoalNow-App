enum AppButtonType { primary, secondary, danger, disabled, social }

enum AppThemeMode { light, dark }

enum PanelSide { left, right }

enum MatchTab { finished, live, upcoming }

enum MatchDetailTab { statistics, lineups, ratings }

enum LineupSide { home, away }