import 'package:flutter/material.dart';

class BuildHighlight extends StatelessWidget {
  const BuildHighlight({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}